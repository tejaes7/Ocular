{
  "name": "@ocular/backend",
  "version": "0.2.0",
  "private": true,
  "description": "Cloudflare Worker that checks prices while the browser is closed.",
  "type": "module",

  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 migrations apply ocular --remote",
    "db:migrate:local": "wrangler d1 migrations apply ocular --local",
    "test": "node --test"
  },

  "dependencies": {
    "@ocular/shared": "*",
    "jose": "^6.2.5"
  },

  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}